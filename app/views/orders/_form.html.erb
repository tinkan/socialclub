
<%= form_for(@order) do |f| %>
  <%= render partial: 'layouts/errors', locals: { model: @order, message: "Votre commande n'a pu être passée :" } %>
  
   <%= f.hidden_field :product_id, :value => params[:product_id] %>
   
    <div class="control-group",>
     
    <%= f.label :"adresse de livraison", :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :address, :autofocus => true  %>
    </div>
  </div>



   <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
